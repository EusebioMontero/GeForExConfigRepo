app.message: Hola mundo desde Security Service

#app.security.jwt.secret: F63B68E11B16E9AD7667E978B1D9H #secreto usado para encriptar el token
#app.security.jwt.expiration: 86400 #el token expira en un dia
#server.port: ${app.security-port}
server.port: 8090
security:
  secret: F63B68E11B16E9AD7667E978B1D9H

eureka:
  client:
    serviceUrl:
      defaultZone: http://${app.eureka-server}:${app.eureka-port}/eureka/
    initialInstanceInfoReplicationIntervalSeconds: 5
    registryFetchIntervalSeconds: 5
  instance:
    leaseRenewalIntervalInSeconds: 5
    leaseExpirationInSeconds: 5